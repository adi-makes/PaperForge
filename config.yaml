llm:
  provider: ollama
  host: http://localhost:11434
  model: llama3.2

embeddings:
  provider: local
  model: BAAI/bge-small-en-v1.5

paths:
  sources_dir: sources
  paper_dir: paper
  assets_dir: assets
  db_path: .paperforge/paperforge.db
